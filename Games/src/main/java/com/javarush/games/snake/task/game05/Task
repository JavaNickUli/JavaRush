Теперь займемся змейкой, а тестовое создание и отрисовку яблока в createGame() удалим.
Создай класс Snake. Змейка будет состоять из нескольких ячеек, т.е. из нескольких GameObject-ов. Кроме того, их
количество может изменяться: оно будет увеличиваться при поедании яблок. Список всех сегментов змейки будем хранить в
поле List<GameObject> snakeParts.
В классе Snake добавь конструктор, который будет создавать сразу три сегмента змейки.

Требования:
В методе createGame() класса SnakeGame не должно создаваться новое яблоко.
Должен существовать публичный класс Snake.
В классе Snake должен существовать один публичный конструктор с двумя параметрами типа int (x и y).
В классе Snake должно быть создано приватное поле List<GameObject> snakeParts, инициализированное при объявлении новым
списком типа ArrayList<>.
В конструкторе должны быть созданы три объекта типа GameObject с параметрами: первый – (x, y); второй – (x + 1, y);
третий – (x + 2, y).
Созданные в конструкторе объекты типа GameObject должны быть добавлены в список snakeParts в порядке: первый, второй,
третий.